apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: canada-tourist
  labels:
    app: frontend
    component: web
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
